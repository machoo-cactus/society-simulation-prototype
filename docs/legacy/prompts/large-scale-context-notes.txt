file for storing prompts and ideas. agents must ignore.

Archived under `docs/legacy/prompts`; this is not active project guidance.

write a plan for allowing create a high detail large scale scenario. it should
have content for all scale levels, focus on fleshing out a chunk of a city
centred around an office(fully defining a neighborhood), include some city level
content e.g. infrastructure, homes for characters. make sure the neighborhood
has realistic layout and content, assume the city is somewhat similar to a
provincial/state capital in a developed country in terms of size, population,
economy, etc. do not worry about whether the content/locations can actually be
accessed/used by the agent just yet. 









in order for large scale agent capabilities, more involved context management
and complex LLM interaction is needed. i am thinking of three major concerns
right now: 1. fleshing out characters fully without dumping agent context with
irrelevant information(characters will continue to get more attributes many of
which will not be relevant at most times, e.g. driving ability is only relevant
when driving is mentioned but most times nothing to do with driving is happening
- multiply this by 100), 2. large scale navigation and planning without
unstructured context dumping(how can an agent go to a location if it doesn't
know what/where it is, but dumping 1000 locations into context is not feasible),
3. online activity, most information flow nowadays happen online. for 1 some sort of retrieval with embeddings seem appropriate(this might be
computationally expensive, think about implementation methods and calculate
costs). for 2, maybe locations can just be tagged...this seems ok but careful
implementation is needed. for 3, this seems extremely complex, ignore this for
now but consider the impact of this on other parts of the system(e.g. when an
character wants to eat outside, the location system will allow the character
agent to basically perceive all relevant locations in their mind, but this is
obviously not realistic where a real person is only aware of a few places but
may search online for new locations). consider these problems carefully and
produce a report on how to best tackle them.